package io.kitsuri.m1rage.navigation

// secondary tab row for HomeScreen - fzul
enum class HomeScreen(
    val route: String,
    val title: String
) {
    HomeScreenA("home_a", "Home A"),
    HomeScreenB("home_b", "Home B")
}